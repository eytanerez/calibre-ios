# P6 — Sell suite (camera-first selling)

First read Docs/specs/shared-rules.md AND mobile-api.md (seller-readiness, connect/account-session, account/listings CRUD + images, shipping-estimate, seller-label PI, listing-imports). Your simulator: **iPhone Air**. Derived data: /tmp/dd-p6. Screenshots: scratchpad/p6/.

You own: Calibre/Features/Sell/, ListingWizard/, BulkImport/. StripeConnect SPM product is already linked. Replace P2's Sell placeholder root in place.

Kit surface: SellerStore (readiness/dashboard/listings/images/estimate/requests/imports), UploadQueue + UploadProgressBoard, models Seller/Listing. Add kit files per shared rules for anything missing (SSN account-session call, label PI, etc.).

## Screens

### 1. SellGate (tab root when seller-readiness.can_list == false)
"Start selling on Calibre" — warm explainer (fee line: "Private sellers keep 92%. Dealers keep 95%."), steps row (Connect payouts → List your watch → Ship when it sells), CTA begins onboarding:
- **SSN step (native)**: masked SecureField auto-formatting 123-45-6789, exact reassurance copy: "Your SSN is sent securely to Stripe to verify your identity. Calibre never stores it — only a one-way fingerprint that keeps previously banned sellers off the marketplace." → POST /stripe/connect/account-session {ssn} (handle error codes: ssn_required, seller_onboarding_blocked, phone_required → human messages).
- **Stripe Connect onboarding**: StripeConnect SDK — EmbeddedComponentManager with the account-session client secret → AccountOnboardingViewController wrapped in UIViewControllerRepresentable, appearance tinted to tokens where the SDK allows. onExit → re-fetch readiness; can_list flips → dashboard. NOTE: this SDK screen may fail in simulator without full provisioning — implement completely, and if it errors at runtime, show a warm fallback ("Finish setting up payouts on the web — your progress is saved.") and report the behavior observed.

### 2. SellerDashboard (tab root when can_list)
From GET /account/dashboard: header "{username}'s shop" serif + "List a watch" primary + overflow (Bulk import status); **Dealer progress card** (X of 10 live listings ProgressCheckpoints-style bar or custom thin bar, "You keep 92% today — dealers keep 95%", or "Dealer active through {date}"); **Action queue** (server-driven prioritized rows with chevrons — respond to offer, buy label, finish drafts — route appropriately); **Buyer requests** rail (open sourcing requests: brand/model/budget/notes, "List this watch" → wizard prefilled + requestId); **Inventory** (SegmentedTabs: All / Needs action / Live / Draft / Pending / Sold / Archived) — rows: thumb, title, StatusBadge (workflow status incl. "Sold — label needed"), #number · price, views/watchers, rejection-reason callout when rejected; swipe actions: Edit / Submit (draft→pending_review w/ confirm) / Archive (confirm); tap → context screen (live → PDP route; sold → SaleDetail); **Recent sales** (last 5, CTA Get label / View sale); **Offers on your watches** section → route .offers (the Offers track owns response UI; seller actions live there — link, don't rebuild).

### 3. ListingWizard (fullScreenCover; new listing / edit / finish-draft modes)
Step eyebrow (Details → Photos → Price → Review), draft-first: create draft on entry (POST /account/listings) and PATCH on field changes debounced; DraftStore JSON in App Support mirrors state for force-quit resume (offer resume on relaunch).
1. **Details**: Brand, Model, Reference (CalibreTextFields; caption "Unusual brands still go to review"), Year (numeric + "Unknown" toggle), Condition: 6 selects (Crystal/Bezel/Bracelet/Clasp/Caseback/Overall — New/Like New/Very Good/Good/Worn) as compact menu pickers with an inline link "How we grade" → info sheet.
2. **Photos — the camera moment**: 6-slot rail (PhotoSlotRing per category: Front, Caseback, Left profile, Right profile, Clasp, Everything included) + "More photos" free slots. Tapping a slot opens **CaptureScreen**: AVCaptureSession (.photo, wide lens, tap-to-focus, flash toggle, grid toggle) with per-category overlay guides (Shape overlays at 35% opacity + one-line instruction: Front "Dial straight-on — fill the circle", Caseback "Flip it over, fill the circle", profiles "Crown side / opposite side straight-on", Clasp "Clasp closed, centered", Full set "Watch, box, and papers together") — shutter (Haptics .capture) → review (Use photo / Retake). HEIC pipeline: downscale longest side 3024px, HEIC 0.8 (JPEG fallback), enqueue UploadQueue immediately (category-tagged) — slot shows live ring progress from UploadProgressBoard, check on done, retry chip on failure. SIMULATOR: no camera — CaptureScreen falls back to PhotosPicker automatically; also add a DEBUG-only "Use sample photos" button that fills all six slots from bundled sample images you generate (solid-color + label placeholders are fine) so the flow is fully testable.
3. **Price**: price field (serif live display), optional notes (≤2000); **payout card** updating live: price − commission ({fee}% from dashboard) − estimated shipping (POST /account/listings/shipping-estimate) = "You'll receive ≈ {payout}".
4. **Review & submit**: hero photo, composed serif title, condition grid, price + payout repeat, photo checklist (6 required states); Submit = verify uploads done (block with clear per-slot status if not) → PATCH status pending_review → success moment ("In review. We'll let you know the moment it's live.") + .success haptic → dashboard refresh. Edit mode resubmits ("Resubmit for approval — your watch leaves the market until re-approved" caption).

### 4. SaleDetail + Fulfillment
- **SaleDetailScreen** (route .sale(orderID)): sale summary (listing, buyer name from shipping address, order StatusBadge, deadlines as CountdownChips), financials SpecList (sale price, commission w/ keep %, estimated payout, payout status + released date), CTA **Get shipping label** when awaiting label.
- **LabelPurchaseFlow**: package details form (L/W/H in + weight lb, carrier-limit validation with human messages, notes) → live quote (debounced) → pay via PaymentSheet on the seller-label PaymentIntent (mobile-api.md) → finalize → **LabelReadyScreen**: Download label PDF (ShareLink/QuickLook), tracking number + copy button, carrier, "Destination: Calibre Authentication Center" address card, fulfillment deadline CountdownChip.

### 5. BulkImportStatus (from dashboard overflow)
Jobs list (GET /account/listing-imports): filename, StatusBadge, progress ("Row X of Y" polling 1.5s while processing), results summary (created/updated/need attention); CalloutBand "Upload and column-map your CSV on the web — then finish drafts here."; **Draft-finishing queue** (completion-queue endpoint): one listing at a time — shows what's missing (photos → CaptureScreen slots; condition; year), "Save & next" rhythm, skip leaves draft.

## Verification
Seller flows need a seller account: register a fresh one (e.g. iosseller.calibre@gmail.com / CalibreiOS123!, unique username) via POST /auth/register (address included, US phone like +12025550143). Then: onboarding gate renders; SSN step → account-session (Stripe TEST mode — use SSN 000-00-0000 which Stripe test accepts; report actual behavior of the Connect component in sim); if can_list stays false, flip it for UI verification ONLY IF a legit dev seam exists — otherwise verify dashboard/wizard against the API as far as permitted and report exactly where the gate stopped you. Wizard: full run with sample photos → six uploads visible in GET /account/listings/{id}/images with correct categories → submit → pending_review. Screenshots light+dark: gate, SSN step, dashboard sections, wizard each step, capture screen w/ overlay (sim fallback OK), photo rail mid-upload, review, sale detail (mock a sold state only if one exists — else skip and say so), bulk status. READ screenshots; fix issues.
